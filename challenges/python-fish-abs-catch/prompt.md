## Абсолютный улов

Вес рыбки не может быть отрицательным! Сделай любое число неотрицательным.

Реализуй функцию `abs_fish(n)`, которая возвращает модуль числа (абсолютное значение).

```python
abs_fish(-5)  # 5
abs_fish(3)   # 3
abs_fish(0)   # 0
```
