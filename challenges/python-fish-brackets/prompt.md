## Скобочный замок

Дана строка из символов `()[]{}`. Нужно проверить, корректно ли закрыты все скобки.

Реализуйте функцию `is_valid_brackets(s)`.

```python
is_valid_brackets("([])") # True
is_valid_brackets("([)]") # False
```
