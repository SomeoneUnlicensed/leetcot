## Кот-эхо

Кот мяукает, и эхо повторяет! Повтори строку `n` раз.

Реализуй функцию `cat_repeat(text, n)`, которая возвращает строку, повторённую `n` раз.

```python
cat_repeat("мяу", 3)  # "мяумяумяу"
cat_repeat("кот", 1)  # "кот"
```
