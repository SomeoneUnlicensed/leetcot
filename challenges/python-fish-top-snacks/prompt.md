## Топ лакомств

Дан список строк и число `k`. Нужно вернуть `k` самых частых строк. Если частоты равны, строка с меньшим алфавитным порядком идет раньше.

Реализуйте функцию `top_snacks(snacks, k)`.

```python
top_snacks(["fish", "milk", "fish", "tea"], 2) # ["fish", "milk"]
```
