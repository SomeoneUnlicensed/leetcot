## Дубликаты заявок (GROUP BY HAVING)

Найдите котов, которые отправили заявку больше одного раза.

Найдите котов, у которых больше одной заявки. Верните `cat` и `duplicates`, отсортируйте по имени.

### Таблица

- `applications` (id, cat, email)

```sql
-- Пример выходных колонок или ожидаемого изменения описан выше
```
