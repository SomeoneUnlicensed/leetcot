## Рыбки по порядку

Кот хочет разложить рыбок по размеру — от маленькой к большой! Отсортируй список.

Реализуй функцию `sorted_fish(bowl)`, которая возвращает новый отсортированный список.

```python
sorted_fish([3, 1, 2])  # [1, 2, 3]
sorted_fish([])          # []
```
