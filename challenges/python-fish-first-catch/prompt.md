## Первая рыбка

Кот достаёт из миски самую первую рыбку! Верни первый элемент списка.

Реализуй функцию `first_fish(bowl)`, которая возвращает первый элемент списка. Список всегда содержит хотя бы один элемент.

```python
first_fish([5, 3, 8])  # 5
first_fish([10])        # 10
```
