## Собрать витрину улова (CREATE TABLE AS)

Создайте итоговую таблицу с суммарным уловом по котам.

Создайте таблицу `catch_summary` с колонками `cat` и `total_fish`, где хранится суммарный улов каждого кота.

### Таблица

- `catches` (cat, fish_count)

```sql
-- Пример выходных колонок или ожидаемого изменения описан выше
```
