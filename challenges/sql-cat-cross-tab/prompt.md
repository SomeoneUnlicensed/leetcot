## Сводка по сменам (SUM CASE)

Соберите дневные и ночные уловы в две колонки.

Сделайте сводку по сменам: в одной строке на кота должны быть `day_fish` и `night_fish`. Если ночной смены нет, значение 0.

### Таблица

- `shifts` (cat, shift, fish_count)

```sql
-- Пример выходных колонок или ожидаемого изменения описан выше
```
