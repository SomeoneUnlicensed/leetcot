## Общий список запасов (UNION)

Соберите единый список названий еды из двух кладовок без дублей.

Объедините `dry_pantry` и `cold_pantry` в один список без повторов. Верните колонку `name`, отсортированную по алфавиту.

### Таблицы

- `dry_pantry` (name)
- `cold_pantry` (name)

```sql
-- Пример выходных колонок или ожидаемого изменения описан выше
```
