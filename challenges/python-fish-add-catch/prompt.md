## Рыбная сумма

Кот поймал две рыбки и хочет узнать их общий вес! Сложи их.

Реализуй функцию `add_fish(a, b)`, которая возвращает сумму `a` и `b`.

```python
add_fish(3, 5)  # 8
add_fish(-2, 2) # 0
```
