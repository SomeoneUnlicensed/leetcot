## Пустые миски в конец

В списке мисок число `0` означает пустую миску. Нужно собрать все пустые миски в конце, не меняя порядок остальных.

Реализуйте функцию `compact_bowls(bowls)`, которая возвращает новый список.

```python
compact_bowls([0, 3, 0, 2, 5]) # [3, 2, 5, 0, 0]
```
