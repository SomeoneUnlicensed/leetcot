## Аккуратная конверсия (NULLIF)

Посчитайте конверсию без деления на ноль.

Посчитайте конверсию `signups / visits`. Если визитов 0, результат должен быть `NULL`, а не ошибка. Верните `channel` и `conversion`.

### Таблица

- `funnels` (channel, visits, signups)

```sql
-- Пример выходных колонок или ожидаемого изменения описан выше
```
