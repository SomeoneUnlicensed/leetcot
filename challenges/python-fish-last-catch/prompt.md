## Последняя рыбка

Кот хочет съесть последнюю рыбку в миске! Верни последний элемент списка.

Реализуй функцию `last_fish(bowl)`, которая возвращает последний элемент списка. Список всегда содержит хотя бы один элемент.

```python
last_fish([5, 3, 8])  # 8
last_fish([10])        # 10
```
