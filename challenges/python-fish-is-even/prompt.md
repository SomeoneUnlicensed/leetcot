## Рыбка-чётность

Кот хочет знать, чётное ли число рыбок в миске! Верни `True`, если число чётное.

Реализуй функцию `is_even(n)`, которая возвращает `True`, если `n` делится на `2` без остатка.

```python
is_even(4)   # True
is_even(7)   # False
is_even(0)   # True
```
