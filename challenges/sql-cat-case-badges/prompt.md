## Раздать рыбные лиги (CASE)

В таблице `scores` лежат очки участников.

Верните `cat` и `league` по правилам:

- `points >= 90` — `золото`
- `points >= 70` — `серебро`
- иначе — `рыбка`

Отсортируйте результат по убыванию очков.

### Таблица

- `scores` (cat, points)
