## Пересчитать налог на рыбу 💰🐟

ЛитКот ввёл новый налог на премиальную рыбу. Все позиции с `tier = 'premium'` должны получить наценку **20%**.

Напишите `UPDATE`-запрос, который обновит цены только для премиальных позиций. Округлите до 2 знаков после запятой.

### Схема таблицы:

- `prices` (id, fish_type, price, tier)

```sql
-- tier: 'standard' или 'premium'
```
