## Бирки на переноски (строки)

В таблице `cats` есть имя кота и цвет переноски.

Верните `cat` и `label`, где `label` имеет формат `имя — цвет`.

Отсортируйте результат по имени кота.

### Таблица

- `cats` (cat, carrier_color)
