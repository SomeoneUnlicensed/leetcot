## Коты с медалями (EXISTS)

Покажите котов, у которых есть хотя бы одна медаль.

Верните имена котов, для которых существует хотя бы одна строка в `medals`. Нужна только колонка `name`.

### Таблицы

- `cats` (id, name)
- `medals` (id, cat_id, title)

```sql

```
