## Сколько рыбок?

В миске плавают рыбки. Нужно посчитать, сколько их всего.

Реализуй функцию `count_fish(bowl)`, которая принимает список чисел и возвращает его длину.

```python
count_fish([5, 3, 8])  # 3
count_fish([])          # 0
```
