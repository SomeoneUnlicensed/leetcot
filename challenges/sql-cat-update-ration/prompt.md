## Увеличить рацион (UPDATE)

Поднимите рацион активным котам.

У активных котов (`active = 1`) рацион нужно увеличить на 10 грамм. Измените таблицу одним `UPDATE`.

### Таблица

- `rations` (cat, grams, active)

```sql
-- Пример выходных колонок или ожидаемого изменения описан выше
```
