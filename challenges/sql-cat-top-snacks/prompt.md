## Топ перекусов (LIMIT)

Выберите три самых популярных перекуса.

Верните три перекуса с максимальным числом голосов. Нужны колонки `name` и `votes`.

### Таблица

- `snacks` (id, name, votes)

```sql

```
