## Сортировка улова (SELECT & ORDER BY) 🐟📈

😺 **ЛитКот напоминает:** Кот Рыжик вернулся с удачной рыбалки. Он поймал много рыбок разного веса и хочет начать ужинать с самой жирной и крупной рыбы!

Напишите `SELECT` запрос, который вернет имя рыбки (столбец `name`) и её вес (столбец `weight`) из таблицы `fishes` для рыб весом **строго больше 100 грамм**, отсортированных по весу **от самых тяжелых к легким** (по убыванию).

### Схема таблиц:

- `fishes` (id, name, weight)

```sql
-- Пример выходных колонок:
-- name | weight
```
