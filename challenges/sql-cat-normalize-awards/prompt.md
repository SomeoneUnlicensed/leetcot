## Нормализовать награды (UPDATE CASE)

Заполните колонку `tier`: от 100 очков — `gold`, от 50 — `silver`, иначе `bronze`. Используйте один `UPDATE` с `CASE`.

### Таблица

- `awards` (cat, points, tier)
