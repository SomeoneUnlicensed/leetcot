## Знак улова

Кот взвесил улов и хочет узнать его знак! Верни `"positive"`, если число больше нуля, `"negative"`, если меньше, и `"zero"`, если равно нулю.

Реализуй функцию `sign_fish(n)`.

```python
sign_fish(5)   # "positive"
sign_fish(-3)  # "negative"
sign_fish(0)   # "zero"
```
