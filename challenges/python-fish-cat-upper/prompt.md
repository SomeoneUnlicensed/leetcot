## Громкий кот

Кот хочет говорить громко! Преобразуй строку в верхний регистр (заглавные буквы).

Реализуй функцию `cat_upper(text)`, которая возвращает строку заглавными буквами.

```python
cat_upper("мяу")  # "МЯУ"
cat_upper("Кот")  # "КОТ"
```
