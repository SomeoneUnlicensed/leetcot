## Острова на карте

Дана прямоугольная сетка из `0` и `1`. Остров — группа единиц, соединенных по вертикали или горизонтали.

Реализуйте функцию `count_islands(grid)`.

```python
count_islands([[1, 1, 0], [0, 1, 0], [1, 0, 1]]) # 3
```
