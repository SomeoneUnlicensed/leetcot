## Рейтинг медалистов (RANK)

Постройте рейтинг котов по количеству медалей.

Постройте рейтинг через оконную функцию `RANK()`. Верните `cat`, `medals`, `place`. При равенстве медалей место должно совпадать.

### Таблица

- `scores` (cat, medals)

```sql

```
