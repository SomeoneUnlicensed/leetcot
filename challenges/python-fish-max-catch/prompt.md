## Самая большая рыба

Кот хочет похвастаться самой большой рыбкой в улове! Помоги ему найти её.

Реализуй функцию `max_fish(bowl)`, которая принимает список чисел и возвращает максимальное из них.

```python
max_fish([3, 7, 2])  # 7
max_fish([-5, -1])   # -1
```
