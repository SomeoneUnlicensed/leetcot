## Кого забыли покормить (LEFT JOIN IS NULL)

Найдите котов, у которых нет ни одной записи о кормлении.

Найдите котов без записей в таблице `feedings`. Верните только колонку `name`, в порядке id котов.

### Таблицы

- `cats` (id, name)
- `feedings` (id, cat_id, food)

```sql

```
